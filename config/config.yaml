db:
  host: "localhost"
  port: 5432
  user: "dba"
  password: "start123"
  dbname: "t3"
  sslmode: "disable"

server:
    port: 8080
    host: "localhost"
    ssl-enabled: false
    ssl-cert: "cert.pem"
    ssl-key: "key.pem"

